package org.VeloMatch.ServerMenager;

import java.io.File;

public class ServerNumber {

    /*
    public static int GetTotalGameNumber(String PLFolder) {
        File directory = new File(PLFolder + "active_Games");
        if (!directory.isDirectory()) {
            return 0;
        }

        File[] files = directory.listFiles();
        int count = 0;

        if (files != null) {
            for (File file : files) {
                if (file.isDirectory()) {
                    count++;
                }
            }
        }

        return count;
    }
     */

}
